PPMD 10th Anniversary website — CURRENT DELIVERY PACKAGE

Build date: 2026-08-27
This is the current verified package. Do not start the server from an older
ai-website or PPMD-site-voices-integrated folder.

Latest motion pass:
- every People/Teams detail panel uses one shared, continuously travelling
  gradient perimeter contained to the tile; the panel itself never rotates;
- Current Focus has a restrained animated atmosphere behind the copy;
- Voices overview has slow rings, pulse and scan motion before hover;
- the solar treatment has no inner scroll container or boxed copy; a visible,
  restrained star source and moving light pool illuminate the comment;
- the long-quote perimeter has a continuously travelling gradient highlight;
- the short-quote treatment writes line by line at a quicker reading pace with
  a visible gradient rail, travelling caret and smooth line-level focus.

This archive contains the source version with the latest workbook import and
the agreed Voices motion treatments.

Included in this version:
- PPMD_Website_Content_Template_V4.xlsm (source workbook)
- 20 final Voices testimonials imported into src/ppmd-content/workbookContent.ts
- 16 final Projects and the current-focus copy imported into the same module
- 3 Teams imported with their current names, counts and descriptions

Data note: the workbook title says 34 professionals, but the current sheet
contains 33 identified records (15 Project Management, 11 Process & Procedures,
7 BPT & Testing). No extra profile has been invented to fill that gap.
- three testimonial effects selected by quote length
- no 1/20 counter in the desktop Voices navigation
- roles shown below names in the Voices field

Preview on Windows:
1. Open PowerShell in this folder (the folder containing package.json).
2. Run: npm.cmd install
3. Run: npm.cmd run dev -- --host 127.0.0.1 --port 5200
4. Open: http://127.0.0.1:5200

Do not start the server from an older ai-website folder. Use this `ppmd-site`
folder as the project root.
